export const fontConfig = {
  display: {
    family: "var(--font-family-display)",
    fallback: "Manrope, Inter, sans-serif",
  },
  body: {
    family: "var(--font-family-body)",
    fallback: "Inter, system-ui, sans-serif",
  },
} as const;
